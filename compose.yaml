services:
  posture-coach:
    build:
      context: .
    image: posture-coach:local
    init: true
    read_only: true
    tmpfs:
      - /tmp:size=64m,mode=1777
    ports:
      - "127.0.0.1:8000:8000"
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
